=tabnav :survey_admin
%br
%br

-if params[:view] != "sort"
  -if params[:encuesta] == "1"
    %h1 Respuestas a encuesta #1 (Símbolos del pais)
  -elsif params[:encuesta] == "2"
    %h1 Respuestas a encuesta #2 (Símbolos del destino)
  -elsif params[:encuesta] == "3"
    %h1 Respuestas a encuesta #3 (Colores del pais)

  -else
    %h1 Respuestas a encuesta #1 (Símbolos del pais)

  = will_paginate(@survey_answers)
  %br

  %table#mytable{:cellspacing=>"0", :summary=>"Projects"}
    %tr
      %th ID
      %th País
      %th Sexo
      %th Símbolo #1
      %th Símbolo #2
      %th Símbolo #3
      %th Símbolo #4
      // %th Email
      %th Acciones
    = render :partial => "survey_answer", :collection => @survey_answers
          
-elsif params[:view] == "sort"
  -if params[:encuesta] == "1"
    %h1 Respuestas a encuesta #1 (Símbolos del pais)
  -elsif params[:encuesta] == "2"
    %h1 Respuestas a encuesta #2 (Símbolos del destino)
  -elsif params[:encuesta] == "3"
    %h1 Respuestas a encuesta #3 (Colores del pais)


  =render :partial => "answers_for_country", :locals => {:country => "México"}
  =render :partial => "answers_for_country", :locals => {:country => "España"}
  =render :partial => "answers_for_country", :locals => {:country => "Colombia"}